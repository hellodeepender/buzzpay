/** @type {import('next').NextConfig} */
const nextConfig = {
  // buzzpay.app — static-friendly Next.js app. Tools are client components;
  // page shells are statically generated so Google indexes each tool instantly.
};
export default nextConfig;
